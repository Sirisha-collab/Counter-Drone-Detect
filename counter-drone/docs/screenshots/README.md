# Screenshots

Drop your captures here with these exact filenames so the links in the root
README resolve:

| File | What to capture |
|------|-----------------|
| `dashboard.png`  | The whole dashboard with several active tracks |
| `map.png`        | The map panel — range rings, trails, an alert-ring contact |
| `tracks.png`     | The active track list, with one row selected |
| `events.png`     | The event log after a few minutes of runtime |
| `training.png`   | Terminal output from `python -m app.ml.train` |

A 1600×900 browser window gives the layout room to breathe.
